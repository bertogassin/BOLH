<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Main app theme -->
    <style name="Theme.GuardianAndroid" parent="android:Theme.Material.NoActionBar">
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
        <item name="android:windowBackground">@color/guardian_background</item>
        <item name="android:colorPrimary">@color/guardian_blue</item>
    </style>

    <!-- Splash screen theme (applied to activity; switches to GuardianAndroid after launch) -->
    <style name="Theme.GuardianAndroid.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_bg</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash_icon</item>
        <item name="windowSplashScreenIconBackgroundColor">@color/splash_icon_bg</item>
        <item name="postSplashScreenTheme">@style/Theme.GuardianAndroid</item>
    </style>

</resources>
