# Local preview values only. Replace every secret before deployment.
APP_ENV=development
JWT_SECRET=local-preview-jwt-secret-change-before-deploy
INTERNAL_SERVICE_TOKEN=local-preview-service-token-change-before-deploy
POSTGRES_PASSWORD=guardian123
MINIO_ROOT_PASSWORD=guardian123
ESCROW_STRICT=false
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# Local client preview only. Never enable on a public production deployment.
NEXT_PUBLIC_DEMO_MODE=0
