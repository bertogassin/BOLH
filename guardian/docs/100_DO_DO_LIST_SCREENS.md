# 100 вещей доделать по экранам (client-web, admin, mobile)

Список составлен по всем экранам: заглушки, смешение языков, отсутствующий функционал, моки, TODO.

---

## Client-web — главная и бронирование

1. **Главная (/)**: только редирект на /booking — нет лендинга/онбординга.
2. **Booking**: единый язык для всех текстов (сейчас часть t(), часть хардкод).
3. **Booking**: выбор места — при отсутствии выбора подставлять дефолтную точку без явного указания пользователю.
4. **Booking**: нет валидации «цена мин ≤ макс» перед отправкой.
5. **Booking**: нет индикации загрузки при отправке заявки (кроме отключения кнопки).
6. **Booking**: пустое состояние «нет заказов» — текст/иллюстрация не унифицированы с остальным приложением.

---

## Client-web — заказы и чаты

7. **Orders (список)**: поиск «Rechercher» — только placeholder, поиск по API не реализован.
8. **Orders**: фильтры по статусу (draft, published, matched и т.д.) не подключены.
9. **Order detail ([id])**: текст «Réservation introuvable» и «Annuler la réservation» — FR при общем RU.
10. **Order detail**: кнопка отмены — нет повторного подтверждения (только confirm).
11. **Order detail**: блок «матч» — нет перехода в чат с охранником из карточки матча.
12. **Orders [id] chat**: заглушка «Сообщения по заказу» — отправка/получение сообщений не реализованы (или не подключены к API).
13. **Create order**: дефолтные координаты 55.75, 37.62 захардкожены — нет выбора на карте/автоопределения.
14. **Create order**: поля даты/времени — нет проверки «конец > начало».
15. **Create order**: required_licenses не отображаются в форме (если нужны для заказа).

---

## Client-web — профиль (общее)

16. **Profile**: «févr. 2026», «Paramètres», «Niveau» — смешение FR с RU по экрану.
17. **Profile**: счётчики заказов/матчей — при ошибке API показывается 0 без сообщения об ошибке.
18. **Profile**: кнопка «Выйти» — двойное нажатие для подтверждения есть, но нет текста «Подтвердить выход».
19. **Profile edit**: лейблы и плейсхолдеры на французском (Prénom, Nom, +33…) при русском интерфейсе.
20. **Profile edit**: сохранение — нет индикации успеха (toast/snackbar).
21. **Profile edit**: нет проверки формата телефона.
22. **Profile change-password**: после смены пароля нет редиректа или сообщения об успехе.
23. **Profile about**: статичный текст «BOLH SECURITY v2.1.0» — версию брать из конфига/package.

---

## Client-web — профиль (подразделы-заглушки)

24. **Profile → Adresses**: «Aucune adresse enregistrée» — нет CRUD адресов, только заглушка.
25. **Profile → Niveau**: только текст «Votre niveau et progression» — нет шкалы/уровня/прогресса.
26. **Profile → Avis**: «Aucun avis pour le moment» — нет списка отзывов и API отзывов.
27. **Profile → Historique des paiements**: «Aucun paiement» — нет списка транзакций и API.
28. **Profile → Supprimer le compte**: «Fonctionnalité à venir» — удаление аккаунта не реализовано.
29. **Profile → Inscription entreprise**: «Fonctionnalité à venir» — регистрация компании не реализована.
30. **Profile → Tableau de bord entreprise**: «Fonctionnalité à venir» — дашборд компании не реализован.
31. **Profile → Vérification**: нет формы подачи документов — только текст «Contactez le support».

---

## Client-web — карты и платежи

32. **Profile → Cartes**: «Aucune carte enregistrée» — список карт с API есть, но добавление карты (profile/cards/add) может не сохраняться в бэкенд (проверить интеграцию).
33. **Profile cards add**: форма добавления карты — проверка номера (Luhn) и привязка к платежной системе не реализованы.
34. **Payments (отдельная страница)**: заглушка «Оплата заказов и способы оплаты будут здесь. Скоро.»

---

## Client-web — настройки, помощь, AI

35. **Settings**: заглушка «Paramètres» + «Notifications, langue, confidentialité. Contenu à venir.»
36. **Settings**: нет переключателя уведомлений, выбора языка, ссылки на конфиденциальность.
37. **Help**: заглушка «Aide et FAQ» + «Questions fréquentes et support. Contenu à venir.»
38. **Help**: нет разделов FAQ и контактов поддержки.
39. **AI (/ai)**: только редирект на /booking и открытие чата — страница не несёт контента.
40. **AIChatPanel**: ответы AI — заглушка или мок (если нет реального бэкенда для AI).

---

## Client-web — уведомления и чаты

41. **Notifications**: при ошибке API показывается пустой список без сообщения «Не удалось загрузить».
42. **Notifications**: «Отметить все как прочитанные» отсутствует.
43. **Chats**: заглушка «Здесь будет чат с подобранными охранниками по вашим заказам. Скоро.»

---

## Client-web — документы и плагины

44. **Documents**: поиск «Поиск…» — фильтрация только на клиенте, без полнотекстового поиска в API.
45. **Documents**: загрузка файла — нет ограничения по типу/размеру в UI и понятных сообщений об ошибке.
46. **Documents [id]**: подпись документа — поле «Введите подпись» без юридически значимой электронной подписи.
47. **Documents planning**: создание плана и задач — сохранение в API (планы/задачи) может быть неполным.
48. **Documents planning [id]**: добавление сотрудника по email — нет проверки существования пользователя.
49. **Documents plugins**: список плагинов с API — создание/редактирование плагина может быть заглушкой.
50. **Documents plugins [id]**: комментарии к плагину — отправка/загрузка через API проверить.

---

## Client-web — карта и навигация

51. **Map**: загрузка заказов/ставок при ошибке API — тихо пустой список, без уведомления пользователя.
52. **Map**: клик по маркеру — нет балуна/карточки с действиями (перейти в заказ/ставку).
53. **BOLHNav / AppNav**: активный пункт и доступ к разделам в зависимости от роли (client/guard/agency) не унифицированы.

---

## Client-web — авторизация и регистрация

54. **Login**: нет «Забыли пароль» и восстановления пароля.
55. **Login**: нет отображения ошибки при неверном пароле (или общее «Ошибка входа»).
56. **Register**: нет согласия с офертой/политикой (чекбокс + ссылки на terms/privacy).
57. **Register**: нет проверки «пароль = повтор» и индикатора сложности пароля.

---

## Client-web — юридические страницы

58. **Legal terms**: краткий текст CGU — нет полной версии и версионирования.
59. **Legal privacy**: страница политики конфиденциальности — проверить наличие и полноту контента.

---

## Client-web — единообразие и UX

60. **Везде**: смешение RU/FR (Connexion, Aucun, Paramètres, Prénom, Nom, Réservation, Aide, Avis, Adresses, Supprimer, Fonctionnalité à venir, Contenu à venir) — привести к одному языку или i18n.
61. **Везде**: пустые состояния — единый компонент EmptyState с иллюстрацией и CTA.
62. **Везде**: ошибки сети — единый компонент ошибки с кнопкой «Повторить».
63. **Везде**: скелетоны загрузки — не на всех экранах (например, список заказов, профиль).
64. **Profile matches**: страница матчей — «Aucun match pour le moment» при пустом списке; проверить подтяжку матчей с API.

---

## Admin — вход и API

65. **Admin login**: TODO — вызов auth API не реализован, редирект на dashboard без проверки.
66. **Admin API users**: stub — возврат мок-списка; подключить к API Gateway / User-service.
67. **Admin API users [id]**: stub — возврат мок-пользователя; подключить к бэкенду.

---

## Admin — дашборд и аналитика

68. **Admin dashboard**: getDashboardStats() — хардкод (totalUsers: 12580, activeOrders: 342 и т.д.); данные из API.
69. **Admin dashboard**: getActiveAlerts() всегда пустой массив — подключить реальные алерты.
70. **Admin dashboard**: блок «Последние матчи» — recentMatches пустой; подгружать из API.
71. **Admin analytics**: все графики на дефолтных моках (defaultOrdersByDay, defaultUserDistribution и т.д.); данные из API.
72. **Admin analytics**: переключатель периода (7d/30d/90d/365d) не меняет запрос к API.
73. **Admin analytics**: экспорт отчётов (PDF/CSV) не реализован.

---

## Admin — пользователи

74. **Admin users**: при ошибке API показывается fallback-мок из двух пользователей — явно показывать ошибку и retry.
75. **Admin users**: кнопка «Экспорт CSV» без реализации.
76. **Admin users**: фильтры (Все/Клиенты/Охранники и т.д.) не отправляются в API (или API не поддерживает).
77. **Admin user [id]**: кнопки «Верифицировать» и «Заблокировать» без обработчиков.
78. **Admin user [id]**: вкладки (активность, заказы, платежи) — контент не реализован или заглушка.

---

## Admin — настройки

79. **Admin settings**: «Раздел в разработке» — нет настроек системы (роли, лимиты, уведомления).

---

## GuardianiOS

80. **OrderDetailViewModel**: TODO — fetch order by id не реализован.
81. **CreateOrderViewModel**: TODO — API call POST /api/v1/orders не реализован.
82. **OrderMapViewModel**: TODO — CLLocationManager, установка региона по местоположению пользователя.

---

## GuardianAndroid

83. **Экраны логина, заказов, создание заказа**: по TASK_TRACKER 1.10 — довести до рабочего состояния (интеграция с API).

---

## Бэкенд и интеграции (по экранам)

84. **Store interface (Go)**: методы Create* не возвращают error — хендлеры не могут отдать 500 при сбое БД; нужен рефакторинг интерфейса.
85. **User-service**: вынести регистрацию/профиль в отдельный сервис (TASK_TRACKER 1.7).
86. **Notification-service**: email при регистрации и при матче (TASK_TRACKER 1.8).
87. **Client-web i18n**: мультиязычность en/ru из guardian/localization (TASK_TRACKER 1.9).
88. **Kafka**: события OrderCreated, BidCreated, MatchCreated (TASK_TRACKER 1.12).
89. **Redis**: кэш сессий, rate limit в Gateway (TASK_TRACKER 1.13).
90. **K8s**: деплой api-gateway, order, bid, matching (TASK_TRACKER 1.14).

---

## Дополнительно по экранам и качеству

91. **Валидация форм**: единая схема (Zod/Yup) для всех форм client-web и вывод ошибок под полями.
92. **Доступность (a11y)**: aria-labels для кнопок и навигации, фокус при модалках.
93. **Мобильная вёрстка**: проверка всех экранов на малых ширинах (320–375px).
94. **Тёмная тема**: в admin и client-web проверить контраст и читаемость.
95. **Мета и SEO**: title/description для всех страниц client-web и admin.
96. **Офлайн**: при потере сети показывать сообщение и не терять введённые данные в формах (где возможно).
97. **Версионирование API**: client-web и admin указывают версию API при запросах (если планируется).
98. **Логирование ошибок**: отправка ошибок фронта в мониторинг (Sentry и т.п.) не настроена.
99. **E2E тесты**: критические сценарии (логин, создание заказа, профиль) не покрыты Playwright/Cypress.
100. **Документация экранов**: в репозитории нет описания экранов (список URL, назначение, основные сценарии).

---

*Список можно использовать как чек-лист: отмечать выполненное и добавлять подзадачи по приоритету.*
