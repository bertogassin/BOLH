[package]
name = "elina-core"
version = "0.1.0"
edition = "2021"
description = "Elina — living brand mascot engine for BOLH platform"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
parking_lot = "0.12"

[dev-dependencies]

[lib]
name = "elina_core"
path = "src/lib.rs"
