name: Guardian Build Gate

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  REGISTRY: ghcr.io

jobs:
  test-rust:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy, rustfmt
      - name: Cache cargo
        uses: Swatinem/rust-cache@v2
      - name: Build and test
        run: |
          cd guardian
          cargo test --workspace --no-fail-fast
          cargo clippy --workspace -- -D warnings
          cargo fmt -- --check

  test-go:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: '1.26.8'
      - name: Test API Gateway
        run: |
          cd guardian/services/api-gateway
          go build ./...
          go vet ./...
          go test ./...

  test-client-web:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: guardian/client-web/package-lock.json
      - name: Install and lint
        run: |
          cd guardian/client-web
          npm ci
          npm run lint
          npm run build:termux
          npm audit --audit-level=high

  test-admin-web:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: guardian/admin/package-lock.json
      - name: Install, lint, build, and audit
        run: |
          cd guardian/admin
          npm ci
          npm run lint
          npm run build
          npm audit --audit-level=high

  validate-compose:
    runs-on: ubuntu-latest
    env:
      INTERNAL_SERVICE_TOKEN: ci-internal-token-not-for-production
    steps:
      - uses: actions/checkout@v4
      - name: Validate Docker Compose
        run: docker compose -f guardian/docker-compose.yml config --quiet

  build-api-gateway:
    needs: [test-rust, test-go, test-client-web, test-admin-web, validate-compose]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      - name: Build api-gateway
        uses: docker/build-push-action@v5
        with:
          context: guardian/services/api-gateway
          file: guardian/services/api-gateway/Dockerfile
          push: false
          tags: guardian/api-gateway:test
          cache-from: type=gha
          cache-to: type=gha,mode=max

  build-matching:
    needs: [test-rust]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      - name: Build matching-engine
        uses: docker/build-push-action@v5
        with:
          context: guardian
          file: guardian/services/matching/Dockerfile
          push: false
          tags: guardian/matching-engine:test
          cache-from: type=gha
          cache-to: type=gha,mode=max
